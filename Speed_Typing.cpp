#include<bits/stdc++.h>
#define for(i,n) for (int i = 0; i < n; ++i)
#define int long long int
#define arr(n) int arr[n]
using namespace std;
int main()
{
int t;
cin>>t;
while(t--){
    string s1,s2;
    cin>>s1>>s2;
    int n1= s1.length();
    int n2= s2.length();
    int count1[256];
    int count2[256];
    if(n1>n2)
    {
        cout<<"IMPOSSIBLE"<<endl;
    }
    else{
        for(i,n1)
        {
            if(s1[i]>'A')
        }

    }
}
    return 0;
}